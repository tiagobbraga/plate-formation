{
  "routes": [
    {
      "method": "GET",
      "path": "/associateds",
      "handler": "associated.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/associateds/count",
      "handler": "associated.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/associateds/:id",
      "handler": "associated.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/associateds",
      "handler": "associated.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/associateds/:id",
      "handler": "associated.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/associateds/:id",
      "handler": "associated.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
